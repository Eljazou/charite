package com.example.charite.repository;

import com.example.charite.entity.CharityActionMedia;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CharityActionMediaRepository extends JpaRepository<CharityActionMedia, Long> {
}